import 'package:flutter/material.dart';

class StudyGroupsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Study Groups Page", style: TextStyle(color: Colors.black, fontSize: 20)),
    );
  }
}
